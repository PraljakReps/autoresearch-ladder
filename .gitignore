# Run outputs — never commit per CLAUDE.md
results/
data/
logs/
*.log

# Per-level autoresearch experiment log (tab-separated, one row per run)
levels/**/results.tsv

# Model weights / large artifacts
*.pt
*.pth
*.ckpt
*.safetensors
*.bin
*.onnx
*.h5
*.npz

# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Venvs (live outside the repo on /workspace/envs/, but guard anyway)
.venv/
venv/
env/.venv/

# OS / editor cruft
.DS_Store
.vscode/
.idea/
*.swp

# Session state — per-pod, ephemeral, not part of the repo
agent-docs/handoffs/
agent-docs/session-handoff.md
